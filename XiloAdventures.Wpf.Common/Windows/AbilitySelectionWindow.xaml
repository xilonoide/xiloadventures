<Window x:Class="XiloAdventures.Wpf.Common.Windows.AbilitySelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar Habilidad"
        Width="400"
        SizeToContent="Height"
        MaxHeight="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="#F5F5F5">

    <Window.Resources>
        <!-- Estilo para botones de habilidad -->
        <Style x:Key="AbilityButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5A5A5A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="8" CornerRadius="8" Background="#2A2A2A" BorderBrush="#444444" BorderThickness="1">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="16">
            <StackPanel>
                <!-- Titulo Habilidades de Ataque -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Habilidades de Ataque"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#FF6B6B"/>
                    <TextBlock Text=" (Magia ofensiva)"
                               FontSize="12"
                               Foreground="#888888"
                               VerticalAlignment="Bottom"
                               Margin="4,0,0,2"/>
                </StackPanel>

                <!-- Lista de habilidades de ataque -->
                <StackPanel x:Name="AttackAbilitiesPanel"/>

                <!-- Titulo Habilidades de Defensa -->
                <StackPanel Orientation="Horizontal" Margin="0,16,0,8">
                    <TextBlock Text="Habilidades de Defensa"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#90EE90"/>
                    <TextBlock Text=" (Magia defensiva)"
                               FontSize="12"
                               Foreground="#888888"
                               VerticalAlignment="Bottom"
                               Margin="4,0,0,2"/>
                </StackPanel>

                <!-- Lista de habilidades de defensa -->
                <StackPanel x:Name="DefenseAbilitiesPanel"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
