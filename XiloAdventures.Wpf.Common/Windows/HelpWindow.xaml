<Window x:Class="XiloAdventures.Wpf.Common.Windows.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ayuda - Comandos Disponibles"
        Icon="/appicon.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="#1E1E1E"
        Foreground="#F5F5F5">
    <Border Margin="20"
            CornerRadius="8"
            Background="#2A2A2A"
            BorderBrush="#444444"
            BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Grid.Row="0"
                       Text="Comandos Disponibles"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#FFD700"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>

            <!-- Contenido en 2 columnas -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Columna izquierda -->
                <StackPanel Grid.Column="0" Width="360">
                    <!-- MOVIMIENTO -->
                    <Border Background="#333333" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="🚶" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="MOVIMIENTO" FontSize="13" FontWeight="Bold"
                                           Foreground="#4FC3F7" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="ir, ve, andar, caminar" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="n, s, e, o, ne, no, se, so, subir, bajar" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="Ej: &quot;ir norte&quot;, &quot;n&quot;, &quot;subir&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- EXPLORACIÓN -->
                    <Border Background="#333333" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="EXPLORACIÓN" FontSize="13" FontWeight="Bold"
                                           Foreground="#81C784" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="examinar, x, mirar, leer" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="Ej: &quot;examinar espada&quot;, &quot;x cofre&quot;, &quot;leer carta&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- INVENTARIO -->
                    <Border Background="#333333" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="🎒" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="INVENTARIO" FontSize="13" FontWeight="Bold"
                                           Foreground="#FFB74D" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="inventario, inv, i" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="coger, tomar, recoger / soltar, dejar, tirar" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="dar, entregar" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="Ej: &quot;coger llave&quot;, &quot;dar moneda a mercader&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- INTERACCIÓN -->
                    <Border Background="#333333" CornerRadius="6" Padding="10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="🔧" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="INTERACCIÓN" FontSize="13" FontWeight="Bold"
                                           Foreground="#BA68C8" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="abrir, cerrar, usar, meter, sacar" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="desbloquear, bloquear (con llave)" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="encender, apagar (antorchas, velas)" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="Ej: &quot;abrir cofre&quot;, &quot;usar llave con puerta&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Columna derecha -->
                <StackPanel Grid.Column="2" Width="360">
                    <!-- CONVERSACIÓN -->
                    <Border Background="#333333" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="💬" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="CONVERSACIÓN" FontSize="13" FontWeight="Bold"
                                           Foreground="#64B5F6" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="hablar, charlar, decir, opcion" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="Ej: &quot;hablar con mercader&quot;, &quot;decir 1&quot;, &quot;opcion 2&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- COMBATE -->
                    <Border Background="#333333" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="⚔️" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="COMBATE Y EQUIPAMIENTO" FontSize="13" FontWeight="Bold"
                                           Foreground="#E57373" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="atacar, golpear, luchar" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="equipar, desequipar, equipo" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="saquear, registrar (cadáveres)" Foreground="#CCCCCC" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="Ej: &quot;atacar goblin&quot;, &quot;equipar espada&quot;, &quot;saquear&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- CRAFTING -->
                    <Border Background="#333333" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="🔨" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="FABRICACIÓN" FontSize="13" FontWeight="Bold"
                                           Foreground="#FFE082" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="fabricar, crear, construir" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="Ej: &quot;fabricar antorcha&quot;, &quot;crear poción&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- OTROS -->
                    <Border Background="#333333" CornerRadius="6" Padding="10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="📋" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock Text="OTROS" FontSize="13" FontWeight="Bold"
                                           Foreground="#90A4AE" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="misiones, esperar (z), dormir, comer, beber" Foreground="#CCCCCC" FontSize="12"/>
                            <TextBlock Text="Ej: &quot;misiones&quot;, &quot;comer manzana&quot;, &quot;dormir&quot;" Foreground="#888888" FontSize="11" FontStyle="Italic" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Fila de consejos (lado a lado) -->
            <Grid Grid.Row="2" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- CONSEJO -->
                <Border Grid.Column="0" Background="#2D3A4A" CornerRadius="6" Padding="10"
                        BorderBrush="#4FC3F7" BorderThickness="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="💡" FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock Text="CONSEJO" FontSize="12" FontWeight="Bold"
                                       Foreground="#4FC3F7" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap" Foreground="#B0BEC5" FontSize="11">
                            Puedes escribir frases naturales:
                            <LineBreak/>"quiero coger la espada del suelo"
                            <LineBreak/>"ve al norte y abre la puerta"
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- IA -->
                <Border Grid.Column="2" Background="#3A2D4A" CornerRadius="6" Padding="10"
                        BorderBrush="#BA68C8" BorderThickness="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="🤖" FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock Text="¿NO TE ENTIENDE?" FontSize="12" FontWeight="Bold"
                                       Foreground="#BA68C8" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap" Foreground="#B0BEC5" FontSize="11">
                            Activa la IA en Opciones para que interprete
                            <LineBreak/>mejor tus comandos con lenguaje natural.
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </Border>
</Window>
