<Window x:Class="XiloAdventures.Wpf.Common.Windows.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entrada" Height="150" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}">
    
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource IpButton}" />
        <Style TargetType="Label" BasedOn="{StaticResource IpLabel}" />
        <Style TargetType="TextBox" BasedOn="{StaticResource IpTextBox}" />
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="White"/>
        
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="0,0,0,10" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Aceptar" Click="Accept_Click" Width="80" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Click="Cancel_Click" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
