<UserControl x:Class="XiloAdventures.Wpf.Controls.CommandInputWithSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- TextBox de entrada del jugador -->
        <TextBox x:Name="InputTextBox"
                 Grid.Column="0"
                 Margin="0,0,8,0"
                 VerticalContentAlignment="Center"
                 Text="{Binding CommandText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 KeyDown="InputTextBox_KeyDown" />

        <!-- Spinner mientras el LLM está pensando -->
        <Grid Grid.Column="1"
              VerticalAlignment="Center"
              HorizontalAlignment="Center">
            <ProgressBar x:Name="Spinner"
                         Width="40"
                         Height="10"
                         IsIndeterminate="True"
                         Visibility="Collapsed" />
        </Grid>
    </Grid>
</UserControl>
