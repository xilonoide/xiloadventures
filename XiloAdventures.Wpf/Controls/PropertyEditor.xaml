<UserControl x:Class="XiloAdventures.Wpf.Controls.PropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Estilo para los encabezados de grupo -->
        <Style x:Key="GroupHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
            <Setter Property="Padding" Value="0,4,0,4"/>
        </Style>
        
        <!-- Estilo para labels de propiedades -->
        <Style x:Key="PropertyLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Margin" Value="0,8,0,2"/>
        </Style>
        
        <!-- Separador de grupos -->
        <Style x:Key="GroupSeparatorStyle" TargetType="Border">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Background" Value="#444444"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer x:Name="MainScrollViewer" VerticalScrollBarVisibility="Auto" Padding="8" PreviewMouseWheel="MainScrollViewer_PreviewMouseWheel">
        <StackPanel x:Name="RootPanel" Margin="0"/>
    </ScrollViewer>
</UserControl>
