<Window x:Class="XiloAdventures.Wpf.Windows.DirectionPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar direcci\u00f3n"
        Icon="/appicon.ico"
        Height="180"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#222222"
        Foreground="White"
        FontFamily="Segoe UI"
        FontSize="15">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Elige la direcci\u00f3n de la salida:"
                   FontSize="16"
                   FontWeight="SemiBold"/>

        <ComboBox x:Name="DirectionCombo"
                  Grid.Row="1"
                  Margin="0,8,0,0"
                  Background="White"
                  Foreground="Black"
                  BorderBrush="#FF555555"
                  HorizontalAlignment="Stretch">
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Padding" Value="4,2"/>
                </Style>
            </ComboBox.ItemContainerStyle>

            <ComboBoxItem Content="norte"/>
            <ComboBoxItem Content="sur"/>
            <ComboBoxItem Content="este"/>
            <ComboBoxItem Content="oeste"/>
            <ComboBoxItem Content="noreste"/>
            <ComboBoxItem Content="noroeste"/>
            <ComboBoxItem Content="sureste"/>
            <ComboBoxItem Content="suroeste"/>
            <ComboBoxItem Content="arriba"/>
            <ComboBoxItem Content="abajo"/>
        </ComboBox>

        <Grid Grid.Row="3"
              Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="OkButton"
                    Grid.Column="0"
                    Style="{StaticResource CommandButtonStyle}"
                    Content="Aceptar"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button x:Name="CancelButton"
                    Grid.Column="2"
                    Style="{StaticResource CommandButtonStyle}"
                    Content="Cancelar"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
