<Window x:Class="XiloAdventures.Wpf.Windows.MusicManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Música"
        Icon="/appicon.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Width="700"
        Height="500"
        MinWidth="600"
        MinHeight="400"
        Background="#1E1E1E"
        Foreground="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Título y botón añadir -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Biblioteca de Música"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    x:Name="AddMusicButton"
                    Style="{StaticResource CommandButtonStyle}"
                    Content="+ Añadir Música"
                    Padding="12,6"
                    Click="AddMusicButton_Click" />
        </Grid>

        <!-- Lista de músicas -->
        <Border Grid.Row="1"
                Background="#2A2A2A"
                BorderBrush="#444444"
                BorderThickness="1"
                CornerRadius="4">
            <ListView x:Name="MusicListView"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="#F5F5F5"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      SizeChanged="MusicListView_SizeChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#F5F5F5"/>
                        <Setter Property="Padding" Value="8"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="{x:Type GridViewColumnHeader}">
                                <Setter Property="IsHitTestVisible" Value="False"/>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                        <GridViewColumn Header="Nombre">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Id}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Id}"
                                               Foreground="#F5F5F5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Tamaño" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SizeFormatted}"
                                               Foreground="#F5F5F5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Duración" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DurationFormatted}"
                                               Foreground="#F5F5F5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource CommandButtonStyle}"
                                            Padding="6,4"
                                            Tag="{Binding}"
                                            ToolTip="Reproducir"
                                            Click="PlayMusicButton_Click">
                                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                                   Text="&#xE768;"
                                                   FontSize="16"
                                                   Foreground="#4EC9B0"/>
                                    </Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource CommandButtonStyle}"
                                            Padding="6,4"
                                            Tag="{Binding}"
                                            ToolTip="Eliminar"
                                            Click="DeleteMusicButton_Click">
                                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                                   Text="&#xE711;"
                                                   FontSize="16"
                                                   Foreground="#E74856"/>
                                    </Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Mensaje cuando no hay músicas -->
        <TextBlock Grid.Row="1"
                   x:Name="EmptyMessageText"
                   Text="No hay música añadida. Haz clic en '+ Añadir Música' para comenzar."
                   FontSize="14"
                   Foreground="#999999"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Window>
